## **Issue Title: [Concise Title of the Task]**

**Milestone:** [Name of the Milestone this issue belongs to, e.g., Initial Setup]

**Assigned Agent (Optional):** [Name or ID of the AI agent if applicable]

**Status:** [e.g., To Do, In Progress, In Review, Done]

---

### **Detail**
* [Detailed bullet point 1 explaining the task. Be specific about what needs to be done.]
* [Detailed bullet point 2. Include any specific logic, conditions, or requirements.]
* [If applicable, describe how relationships with other objects should be handled (e.g., "For [relatedObject], API route will take an array of [relatedObject]: [{id: string, attribute: boolean}]")]
* [Specify behavior for updates if collections are involved (e.g., "Update will replace with any [relatedObjects] in the payload, do nothing if undefined, and clear it if it gets an empty array")]
* [Mention specific validation requirements, e.g., "Implement basic validation for input data (e.g., required field is [fieldName])."]

---

### **References**
* [Path to relevant type definition files, e.g., `common/types/ObjectName.ts`]
* [Path to data model or database schema files, e.g., `dev/project/data/db.json`]
* [References to existing code files that provide examples or context, e.g., "The `valert-firebase/functions/api/users/crudLogic.js` and `valert-firebase/functions/api/channels/crudLogic.js` files provide examples of how CRUD logic can be structured."]
* [Links to any relevant documentation or external resources.]

---

### **Acceptance Criteria**
* [Specific, measurable, achievable, relevant, and time-bound (SMART) criteria 1. e.g., "Backend functions for `createObjectName`, `getObjectName`, `updateObjectName`, and `deleteObjectName` are implemented."]
* [SMART criteria 2. e.g., "Functions correctly interact with the Firebase Realtime Database."]
* [SMART criteria 3. e.g., "Input validation for [specific fields] is in place."]
* [SMART criteria 4. e.g., "Each CRUD operation is successfully tested (manually or via automated tests)."]
* [SMART criteria 5. e.g., "Data handling for [relatedObject] arrays (create, update with payload, update with empty array, update with undefined) is correctly implemented and tested."]

---